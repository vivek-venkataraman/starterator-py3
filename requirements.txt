# Core dependencies for Starterator
# Database connectivity (Python 3 compatible)
pymysql>=1.0.0

# Bioinformatics libraries
biopython>=1.78

# PDF generation and manipulation
reportlab>=3.5.0
PyPDF2>=2.0.0

# Web scraping and HTTP requests (for annotation features)
requests>=2.25.0
beautifulsoup4>=4.9.0

# GUI dependencies (optional - only needed for GUI mode)
# Note: GTK+ and PyGObject need to be installed system-wide
# On Ubuntu/Debian: sudo apt-get install python3-gi python3-gi-cairo gir1.2-gtk-3.0
# On macOS with Homebrew: brew install pygobject3 gtk+3
# On Windows: install from https://pygobject.readthedocs.io/en/latest/getting_started.html
# PyGObject>=3.30.0  # Uncomment if you need GUI functionality
